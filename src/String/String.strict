implement Sequence<Character>

has characters Character[]

factory From(number Number)
  return parseNumber(number) 

method Iterate() returns Iteration<Character>
  return characters.Iterate()

method ListCharacters() returns Character[]
  return characters